## Write a function which takes two arguments: a and b and returns the multiplication of them: a*b. Assign it to a variable named: f. using python
#Aim 
```
To Write a function which takes two arguments: a and b and returns the multiplication of them: a*b. Assign it to a variable named: f. using python
```
#algorithm.
```
1. Start.
2. Define a function that takes two arguments `a` and `b`.
3. Return the product `a * b`
4. Assign the function to a variable `f`.
```
##program
```
f=lambda a,b:a*b

a=int(input())
b=int(input())

print(f(a,b))    
```

#output
![image](https://github.com/user-attachments/assets/6ac915f6-5bb9-4728-938c-ceb65ba39a68)



#result
```
The expected output is Achieved.
```
